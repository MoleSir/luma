[package]
name = "mnist"
version = "0.1.0"
edition = "2024"

[dependencies]
luma-tensor = { path = "../../luma-tensor" }
luma-dataset = { path = "../../luma-dataset" }
luma-nn = { path = "../../luma-nn" }
luma-io = { path = "../../luma-io" }
luma-optim = { path = "../../luma-optim" }
anyhow = "1.0"